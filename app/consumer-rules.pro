# Consumer ProGuard rules — empty by default. Liquid Glass overlays only
# need to keep the Compose-generated stable lambdas, which Compose
# already protects via its own consumer rules pulled from the BOM.
